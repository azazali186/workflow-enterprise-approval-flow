apiVersion: v1
kind: Service
metadata:
  name: approval-flow
  namespace: approval-flow
  labels:
    app.kubernetes.io/name: approval-flow
spec:
  selector:
    app: approval-flow
  ports:
    - name: http
      port: 8080
      targetPort: http
